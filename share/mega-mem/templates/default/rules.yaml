name: rules
description: Cross-harness behavioral rules. Markdown files at any depth; loaded for every harness.

# No default subfolders — rules are loaded universally. If you want
# harness-targeted rules later, frontmatter-based audience filtering
# is the planned mechanism (see FEATURES.md v1.x). Until then, organize
# by topic with your own subfolders if you like.
folders: []
